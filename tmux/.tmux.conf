# Reload config
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# Faster key repeat
set -g repeat-time 200

# Mouse support
set -g mouse on

# True color
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*:Tc"

# Vim-style pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Make resize more chonky, M == Meta == Alt key, and ctrl for controlled
bind -n C-Left  resize-pane -L 5
bind -n C-Right resize-pane -R 5
bind -n C-Up    resize-pane -U 5
bind -n C-Down  resize-pane -D 5
bind -n M-Left  resize-pane -L 15
bind -n M-Right resize-pane -R 15
bind -n M-Up    resize-pane -U 10
bind -n M-Down  resize-pane -D 10

# Split shortcuts
bind | split-window -h
bind - split-window -v

# Start window numbering at 1
set -g base-index 1
setw -g pane-base-index 1

# Better status bar
set -g status-interval 5
set -g status-left "#S"
set -g status-right "%Y-%m-%d %H:%M"

# Don't delay ESC (important for nvim)
set -sg escape-time 10

# Reset prefix to ctrl-space
unbind C-b
set -g prefix C-space
bind C-space send-prefix
bind C-a send-prefix # backup option ctrl-a
